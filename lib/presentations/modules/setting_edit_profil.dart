import 'package:flutter/material.dart';


class SettingEditProfilPage extends StatelessWidget {
  const SettingEditProfilPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("settings edit profil Page",),
      ),
    );
  }
}