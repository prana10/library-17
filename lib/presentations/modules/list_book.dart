import 'package:flutter/material.dart';


class ListBookPage extends StatelessWidget {
  const ListBookPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("List book Page"),
      ),
    );
  }
}